eureka:
  client:
    serviceUrl:
      defaultZone: http://ipcm-eureka-server:8761

server:
  port: 8080
# routing for user service
zuul:
  routes:
    user:
      path: /api/v1/**
      url: http://ipcm-user-service:8000/